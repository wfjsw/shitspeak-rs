Added quorum-never-forms regression `strict_quorum_never_forms_times_out` in `src/s2s/replications/integration_tests/scenarios.rs`. It uses new harness helper `ReplCluster::build_full_mesh_with_config` to set short `ReplicationConfig` timings: delivery tick 25ms, propose_ttl 500ms, pending_propose_ttl 2s. The test builds a 4-node healthy full mesh, drops the next replication `Data` frame from nodes 3 and 4 inbound to coordinator node 1, then proposes from node 1. Membership/routing remain healthy, but fast quorum never forms; expected result is `Err(ReplicationError::ProposeTimeout(cfg.propose_ttl()))`, no repo advances version, and full routing still holds. Verified with `cargo test strict_quorum_never_forms_times_out`. Existing `strict_quorum_lost_on_partition` still passes and covers membership-shrink failure returning `QuorumLost`. Hostile connected-link test still passes. `cargo fmt --check` passed. Cargo emits non-fatal Windows incremental compilation finalization Access denied warning.