MoQ transport scaffold continued on 2026-05-19. Implemented/finished: optional Cargo `moq` feature with `moq-lite`, `moq-native`, `hang`; `[web.moq]` config including `enabled`, `listen`, `public_url`, optional `cert_path`/`key_path`, `max_speaker_tracks`, `audio_bitrate`; gateway transport metadata advertises MoQ only when enabled and listener is configured; server spawns a separate MoQ WebTransport listener and passes main TLS cert/key as fallback. `src/web/moq.rs` contains direct MoQ session runtime using `/web/moq`, `control/up`, `control/down`, `audio/up/mic`, `audio/down/slot/{n}`, and Hang catalog track `catalog.json`. Fixed important follow-up issues: inbound audio decodes Hang frames first while preserving legacy SSMA terminator envelope by magic check; outbound audio task starts after auth and owns the client's TCP voice receiver; MoQ session cleanup removes the allocated local client on exit/errors; subscribe both inbound tracks from one announced broadcast. Tests passing: `cargo fmt --check`, `cargo test --lib`, `cargo test web:: --lib`, `cargo test --features moq web:: --lib`, `cargo test --features moq web::moq --lib`, `cargo check --lib`, `cargo check --features moq --lib` (checks emitted known Windows incremental finalization warning but exited 0). SDK Node tests not run because `node` is not on PATH. Browser MoQ adapter remains a placeholder that opens WebTransport then throws asking for `moqAdapterFactory`; fake MoQ SDK tests were added but unverified locally due missing Node.