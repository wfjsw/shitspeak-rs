Added regression test `strict_unreachable_node_during_replication_survives` in `src/s2s/replications/integration_tests/scenarios.rs`. It builds a 4-node strict replication cluster, registers `channels`, shuts down node 4, proposes from node 1 while the stale target set may still include the stopped node, and asserts the proposal task does not hang or panic. Expected behavior: survivor fast quorum commits `Ok(1)`, nodes 1-3 reach current_version 1, stopped node remains at 0. Verified with `cargo test strict_unreachable_node_during_replication_survives`; Cargo emitted a non-fatal Windows incremental compilation finalization Access denied warning. `cargo fmt --check` passed.