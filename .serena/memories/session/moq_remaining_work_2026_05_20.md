Continued MoQ transport work on 2026-05-20. User asked to work on remaining items 1, 2, 4, and 6, and requested Node at `C:\Users\synopses0130\AppData\Roaming\fnm\node-versions\v22.22.2\installation\node.exe` with elevated permissions. Verified SDK Node tests with that binary; all 8 SDK tests pass. Added Rust MoQ outbound/server-to-browser coverage in `src/web/moq.rs`: extracted `write_audio_frame_to_track`, added `outbound_audio_task_maps_tcp_voice_to_speaker_slot_events_and_hang_frames`, and `outbound_audio_track_writes_hang_frame_bytes`. These cover normal queued server voice becoming `speaker_assigned`, `voice_segment_start`, Hang-framed slot audio, and `voice_segment_end`. Added browser smoke harness files `web/demo/moq-smoke.html` and `web/demo/moq-smoke.js` for Chromium capability checks, explicit MoQ connection/auth, mic, PTT, catalog/status/events, and audio track attach. Verification: `cargo fmt --check`, `cargo test --features moq web:: --lib`, and elevated Node SDK tests all pass. There is no existing Playwright/package setup in repo, so no automated Chromium run was added or executed.