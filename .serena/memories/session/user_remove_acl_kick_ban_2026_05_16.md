UserRemove kick/ban ACL enforcement added on 2026-05-16. `src/client/handlers/user_remove.rs` now computes `is_ban`, maps to root-channel `ACLPermissions::Ban` or `ACLPermissions::Kick`, checks `compute_permissions_for_client(server, sender, 0)`, and returns `PermissionDenied::for_permission(session, Some(0), required_permission)` before both cross-owner S2S dispatch and local target removal. Focused tests added in `src/integration_tests/scenarios/mod_actions.rs`: `non_mod_cannot_kick_other` and `non_mod_cannot_ban_other`. `cargo test mod_actions` passed; cargo emitted a non-fatal Windows incremental compilation finalization warning: Access is denied.