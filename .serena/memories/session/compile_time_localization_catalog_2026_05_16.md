Compile-time localization catalog implemented 2026-05-16. `translations/client_text.json` stores client-facing localized text by language (`en`, `es`, `fr`, `de`, `zh`) and sections (`text`, `reject`, `deny`). `build.rs` now uses build-dependencies `serde` and `serde_json` to parse and validate that every configured language has every known key, then writes generated Rust match functions to `$OUT_DIR/localization_catalog.rs`. `src/localization.rs` includes that generated file and keeps stable helper APIs: `text`, `channel_does_not_exist`, `reject_reason`, and `permission_denied_reason`. Verification: `cargo fmt`, `cargo test auth_`, and `cargo test permission_denied_uses_authenticated_language` passed. Cargo still emits the known non-fatal Windows incremental cleanup warning: Access is denied.