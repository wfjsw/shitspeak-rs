Localized server-generated client text added 2026-05-16. New `src/localization.rs` defines `Language`, text catalog, reject and permission-denied reason helpers. `AuthenticateResult` now includes `language: Language`, and `Authenticator` has a default async `language(username, auxiliary_data)` hook used before authentication so failed auth responses can use a known user's language. `ClientLocalState` stores language; `Client` exposes `language()`/`set_language()`. `AuthRejection::new` now fills default reason text based on `RejectType` (TODO resolved); `new_with_language` and `localized` support per-client language. Central handler dispatch localizes default auth rejections and default permission denials before writing to clients. Stable explicit handler denial strings in ACL/channel remove/channel state/user state now use localization helpers. Integration harness supports `register_user_with_language`; tests added for Spanish wrong-password Reject and Spanish PermissionDenied for missing channel. Verification: `cargo fmt`, `cargo test auth_`, `cargo test permission_denied_uses_authenticated_language` passed. Cargo emitted non-fatal Windows incremental compilation cleanup warning: Access is denied.