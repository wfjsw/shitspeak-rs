2026-05-22: Adjusted `src/s2s/transport/manager.rs` so `ConnectionManager::add_address` and `add_address_seen_at` only emit `"derived peer address candidate using observed remote IP"` after `PeerState::add_address*` returns true. This suppresses the manager debug log for derived peer address candidates when the peer address set is unchanged; `last_seen` updates in `add_address_seen_at` can still occur without that derived-candidate log.