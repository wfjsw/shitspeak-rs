Implemented actual local TCP/TLS disconnect for `UserRemove` on 2026-05-16. `Client` now owns a watch-based disconnect signal; `Client::disconnect()` sets it and gracefully shuts down the TLS write half. `Server::handle_incoming_connection` selects on `client.disconnected()` and exits the connection loop on local disconnect requests, which drops the read half and completes socket closure once outstanding refs end. `src/client/handlers/user_remove.rs` removes the target from `ClientRepository` then calls `disconnect()` and logs shutdown failures without failing the moderator handler. `src/s2s/mod.rs` does the same in owner-side `apply_user_remove_patch` so cross-node moderation also closes the target connection on the owner. Integration harness has `TestClient::recv_closed`; `mod_kicks_other` asserts Bob's TCP connection closes after kick. Verification: `cargo fmt` and `cargo test mod_actions` passed; Cargo emitted the known non-fatal Windows incremental cleanup warning: Access is denied.